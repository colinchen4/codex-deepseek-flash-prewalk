interface:
  display_name: "Prewalk"
  short_description: "DeepSeek execution with SOL review"
  default_prompt: "Use $prewalk to prewalk this coding task in SOL, execute it with the managed DeepSeek executor, and return to SOL for final review."
